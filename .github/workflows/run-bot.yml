name: Run Trading Bot (Continuous Mode)

on:
  schedule:
    # Ejecuta cada hora (a los minutos 00)
    - cron: "0 * * * *"
  workflow_dispatch: # permite ejecución manual
    inputs:
      reason:
        description: "Motivo del run manual"
        required: false
        default: "Ejecución manual"

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: 🚀 Iniciando Trading Bot...
        run: echo "🚀 Iniciando Trading Bot (Continuous Mode)..."

      # Clona el repositorio
      - name: 📥 Checkout repo
        uses: actions/checkout@v4

      # Configura Python
      - name: 🐍 Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # Instala dependencias
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          echo "✅ Dependencias instaladas."

      # Test de variables para debugging (no muestra valores)
      - name: 🧩 Check environment variables
        run: |
          echo "GOOGLE_CREDS_JSON set: ${{ secrets.GOOGLE_CREDS_JSON != '' }}"
          echo "GOOGLE_CREDS set: ${{ secrets.GOOGLE_CREDS != '' }}"
          echo "SPREADSHEET_ID set: ${{ secrets.SPREADSHEET_ID != '' }}"
          echo "ALPHA_VANTAGE_KEY set: ${{ secrets.ALPHA_VANTAGE_KEY != '' }}"
          echo "✅ Variables verificadas."

      # Ejecuta el bot
      - name: ▶️ Run bot.py
        env:
          GOOGLE_CREDS_JSON: ${{ secrets.GOOGLE_CREDS_JSON }}
          GOOGLE_CREDS: ${{ secrets.GOOGLE_CREDS }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
          ALERT_DEFAULT: ${{ secrets.ALERT_DEFAULT }}
          ALERT_ES: ${{ secrets.ALERT_ES }}
          ALERT_DKNG: ${{ secrets.ALERT_DKNG }}
          ALPHA_VANTAGE_KEY: ${{ secrets.ALPHA_VANTAGE_KEY }}
        run: |
          echo "🏁 Ejecutando bot.py"
          python bot.py || echo "⚠️ Error detectado durante la ejecución."

      # Si el bot falla, registra el error y programa reintento
      - name: 🔁 Auto-retry if failed
        if: failure()
        run: |
          echo "⚠️ El bot falló. Se reintentará en la próxima ejecución."
          echo "⌛ Manteniendo logs limpios para próxima iteración."

      # Limpieza de caché (evita saturación)
      - name: 🧹 Clean up
        run: |
          rm -rf __pycache__
          echo "🧽 Limpieza finalizada."
